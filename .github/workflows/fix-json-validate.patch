*** Begin Patch
*** Update File: .github/workflows/json-validate.yml
@@
-      - name: Install dependencies
-        run: npm ci --no-audit --no-fund
+      - name: Install dependencies
+        # Use `npm install` instead of `npm ci` to avoid lockfile mismatch errors when package.json has been updated without regenerating package-lock.json.
+        # This allows the validate job to succeed even if the lockfile is out-of-sync.
+        run: npm install --no-audit --no-fund
*** End Patch
