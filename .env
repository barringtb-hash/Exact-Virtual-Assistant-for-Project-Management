VITE_TEMPLATE_BASE_URL=/templates/

# Intent-Only Extraction Mode
# When true, extraction only runs when user explicitly requests "create charter" (default: true)
# When false, extraction runs automatically on every message (not recommended)
# VITE_INTENT_ONLY_EXTRACTION=true

# Charter Wizard Feature Flags
# =================================
# For PM-friendly sequential field collection, enable the wizard:
# VITE_CHARTER_WIZARD_VISIBLE=true
#
# The wizard provides:
# - Sequential field-by-field prompting
# - Real-time validation
# - Skip/back navigation
# - Progress tracking
#
# Set to true to enable the Charter Wizard UI (default: false)
VITE_CHARTER_WIZARD_VISIBLE=false

# Auto-Extraction Feature
# When true, shows "Auto-fill from uploaded scope" button (requires wizard=true)
# When false, only manual field entry is allowed (default: false, prevents accidental auto-fill)
VITE_AUTO_EXTRACT=false

# Conversation Persistence
# When true, saves conversation snapshots to disk for session resumption
# When false, conversation state only persists in memory (default: false)
# VITE_CHARTER_CONVERSATION_PERSIST=false
