*** Begin Patch
*** Update File: .github/workflows/ci.yml
@@ jobs:
       test:
         name: Build & Test
         runs-on: ubuntu-latest
         timeout-minutes: 30
         strategy:
           fail-fast: false
           matrix:
             node-version: [22]
@@ steps:
-      - name: Install Cypress system deps
-        run: bash scripts/install-cypress-deps.sh
-
-      - name: Lint
-        run: npm run lint --if-present
-        env:
-          CI: true
-
-      - name: Unit/Integration tests
-        run: npm test --if-present
-        env:
-          CI: true
-
-      - name: Build Vite app
-        run: npm run build
-
-      - name: Start preview on strict port
-        run: |
-          npm run preview -- --host --port 5173 --strictPort &
-          npx --yes wait-on http://127.0.0.1:5173
-
-      - name: Cypress E2E (headless via Xvfb)
-        run: xvfb-run -a npm run e2e:ci
-        env:
-          CI: true
+      - name: Lint
+        run: npm run lint --if-present
+        env:
+          CI: true
+
+      - name: Unit/Integration tests
+        run: npm test --if-present
+        env:
+          CI: true
+
+      # Use the official Cypress GitHub Action to run end‑to‑end tests.
+      # The action installs the Cypress binary and all required system
+      # dependencies, builds the app, starts the preview server on a fixed
+      # port, waits for it to be reachable, and then runs the Cypress suite.
+      - name: Cypress end‑to‑end tests
+        uses: cypress-io/github-action@v6
+        with:
+          build: npm run build
+          start: npm run preview -- --host --port 5173 --strictPort
+          wait-on: http://127.0.0.1:5173
+          browser: electron
+        env:
+          CI: true
*** End Patch
